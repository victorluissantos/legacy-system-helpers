💡 Como usar:

    Crie um arquivo com a query que deseja buscar, ex: query.sql

    Salve o script acima como sql_finder.py

    Execute com:
```
python3 sql_finder.py /caminho/para/seu/sistema query.sql
```